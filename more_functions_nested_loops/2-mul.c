#include "main.h"

/**
 * mul - Function to multiplies two integers and returns the result
 * @a: First integer to add
 * @b: Second integer to add
 * Return: Nothing
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
